const styles = theme => ({
	sign: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: theme.palette.secondary.main,
    },
    label: {
        color: 'white',
        fontWeight: 900
    }
});

export default styles;
